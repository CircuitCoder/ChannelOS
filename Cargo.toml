[package]
name = "channel_os"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[profile.dev]
panic = "abort"
debug = true

[profile.release]
panic = "abort"
debug = true

[dependencies]
riscv = "0.8.0"
